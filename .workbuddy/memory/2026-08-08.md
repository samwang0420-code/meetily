# 2026-08-08 — §94.1 TranscriptSettings 硬编码修复

## 触发
哥 13:40 截图: v0.8.6 已生效但 Transcript Model 下拉仍 v0.7 W2.5 列表 (SenseVoice 23 段 / Paraformer 10 段 / Parakeet 旧推荐).

## 根因
§90 commit fda59cd 改了 useTranscriptionModels.ts hook (新 §90 决策列表), 但 TranscriptSettings.tsx 里硬编码 SelectItem 没改. 典型 "AGENTS.md § 章节 ≠ 代码" 案例. §94 audit 检查了后端 + hook, 但漏了前端硬编码.

## 修复 (§94.1)
- TranscriptSettings.tsx:138-141 改 2 个 provider (sherpa_funasr_nano / sherpa_paraformer), 删 parakeet
- TranscriptSettings.tsx:73 modelOptions dict 删 parakeet
- TranscriptSettings.tsx:175-185 第 2 个 Select 加 prettyName 漂亮名字
- TranscriptSettings.tsx:196 描述 "23 段按句切" → "228MB" + 加 FunASR-Nano Pro
- scripts/audit_codebase.py 加 check_hardcoded_model_list() 检测前端硬编码 W2.5 模式

## 教训
audit 检查代码广度要扩到前端硬编码 (不光 hook / 后端). 任何"§ 决策改了 X" 必须 grep 整个 codebase 看是否还有其他地方有旧硬编码.

## 验证
- pnpm run build ✅
- cargo build --release 1m 28s, binary 72.6MB mtime 12:17
- sync_app_bundle.sh hash 9043619e53c8 一致
- audit 0 errors / 0 warns / 60 info
- guard 84/84 PASS
- .app bundle 启动 PID 38778 (12:18)
- commit 4a9a300 落地

## 双写
- outputs/95 ↔ Obsidian 项目/3-离线会记/95
- 后续 §94 audit 升级, 加 check_hardcoded_model_list
