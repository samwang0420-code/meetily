# 2026-08-07 工作日志

## §92 代码漏根因审计 (用户触发)
用户原话: "我现在发现每次大的变动之后代码都会漏，功能会恢复到以前的几个版本之前的功能上，这不是我想要的"

### 真实发现
1. **§91 14 项核心功能全部真实** (P0-A/B/P1-A/B/C/D/P2-A/B/C + 热词 + 5 bug) — 验证全通过
2. **§62 三联优化完全回退** — outputs/62 文档存在，但 commit `835a3c3`, `1f42580`, `d99b0b2` 在 git 历史里 0 命中。当前代码 sherpa_daemon.rs:113 仍 `Mutex<Option<SherpaHandle>>` 单 daemon，import.rs 无 hardlink，processor.rs:12 仍 `1200`
3. **§63 provider 没改** — retranscription.rs:474 仍 `sensevoice-zh`
4. **§90 friendlyImportTitle 全仓不存在** — 全仓 grep 0 命中
5. **guard 脚本盲点** — 64/64 PASS 是假象，§62 §63 §90 都没 anchor，所以即便回退也是 PASS
6. **import.rs 整个转写流程不走 sherpa_daemon** — 即便 §63 改对，import 也受益不了

### 根因 (5 个)
- AGENTS.md § 章节 ≠ 代码 commit (outputs 写了但 commit 失败/丢失)
- outputs 文档写在 commit 之前
- guard 只检查文字锚点不验证功能
- 用户只看 binary 不验证代码
- 分支策略混乱 (perf/summary-map-concurrency vs fix/polling-30min)

### 交付
- outputs/92-代码漏根因审计-AGENTS.md与代码脱节-2026-08-07.md (完整审计报告)
- P0-P2 修复优先级清单

### 教训 (供未来 § 使用)
- guard --strict 必须每次 release 前跑
- guard 需要升级：加 §62 §63 §90 anchor，加功能验证（daemon count, max_tokens 值）
- commit message 写"修 §X"前必须 git show 验证 commit 真存在
- 用户验收必须含"git log --grep §X" 看 commit 真在主线

## §92.1 双写规则立 (用户触发)
用户原话: "我做这个项目过程也同步写 obsidian，你也保持这个规则，每次修改和决策都需要双写。目录在 Obsidian Vault"

### 双写路径
- `outputs/§X-...md` ↔ `~/Documents/Obsidian Vault/项目/3-离线会记/§X-...md`
- `AGENTS.md` ↔ Obsidian AGENTS.md
- `README.md` ↔ Obsidian README.md (修正路径错)
- 不双写: 决策日志-当前.md (污染严重)

### 双写状态 (2026-08-07 11:17)
- ✅ outputs/91 ↔ Obsidian 91
- ✅ outputs/92 ↔ Obsidian 92 (含 §92.1 双写规则章节)
- ✅ AGENTS.md ↔ Obsidian AGENTS.md
- ✅ README.md ↔ Obsidian README.md (主仓库路径 /Users/wangwei/Documents/meetily/ → /Users/wangwei/Documents/离线会记/)

### 写入命令
```bash
cp outputs/§X-*.md "$HOME/Documents/Obsidian Vault/项目/3-离线会记/§X-*.md"
diff -q outputs/§X-*.md "$HOME/Documents/Obsidian Vault/项目/3-离线会记/§X-*.md"
```

## §93 v0.8.6 P0 修复 (用户截图驱动, 2026-08-07 12:18)
用户原话: "我现在打开的软件是这样的...我要看到最新的软件，你去实现它"

### 真实情况
- 用户截图显示 v0.8.5 (但 git HEAD caad987 是 v0.8.6) — UI 字符串漏改
- §62 §63 §90 漏代码: 之前 §92 误判 §90 不存在 (实际在 ImportAudioDialog.tsx)
- 8 个 § 真在代码里 (§91 P0-P2)

### §93 修复 (7 项, commit 2fe96d7)
1. **UI 版本号 v0.8.5 → v0.8.6** (7 处: Sidebar/HomeDashboard/register/en.ts)
2. **§62 A 多 daemon** (sherpa_daemon.rs 改 Vec<Mutex<Option<SherpaHandle>>> + AtomicUsize round-robin + env MEETILY_SHERPA_DAEMONS + 3 单测)
3. **§62 B.1** import.rs hard_link 优先 + 跨卷 fallback
4. **§62 B.3** decoder.rs tempfile_in(temp_dir) (1.08GB wav 30s → 6s)
5. **§62 C** max_tokens 1200 → 800 + 3 单测
6. **§63** provider sensevoice-zh → funasr-nano-zh
7. **guard 升级** 64 → 75 anchor (+11 §62/§63/§90/UI)

### §37 6 步硬闸门
- cargo check 0 errors (26 §18 warnings)
- cargo test 326/326 PASS (skip 3 system_audio SCK 弹窗测试)
- pnpm run build OK
- cargo build --release 1m 39s, binary 72.5MB mtime 12:18
- guard 75/75 PASS
- binary 启动 OK, no panic, log 显示 "Topic dossier nightly scheduler started" + "Notification system initialized"

### 性能
- §62 A: 1.5h 录音 import 11min → 4min (3.7x, 3 daemon 默认)

### 已知 open
- import.rs 仍走 whisper engine, 不走 sherpa_daemon (§63 P1 跟进)
- tauri.conf.json identifier cn.lixianhuiji.app 未改 (P1, 需旧路径迁移)
- 3 system_audio 测试 skip (macOS SCK 限制)

## §93.1 macOS .app bundle 自动同步 (用户截图驱动)
用户原话 + 截图: 设置 → 转录 → 模型列表仍是 0.6 (SenseVoice 23段/Paraformer 10段/Parakeet), 但 git HEAD 是 §90 改过的 0.8 列表

### 根因
- §90 commit fda59cd 手造了 .app bundle (target/release/言镜 AI.app/Contents/MacOS/言镜 AI)
- cargo build --release 只更新 target/release/meetily (workspace root target/)
- .app bundle 内部 binary 独立, 不是 symlink/hardlink, hash 不同 (6bc06d8 vs 4b2373f)
- 用户跑 .app bundle 时看到 1:50 旧 binary, §90 改的 0.8 模型列表漏

### 修复 (commit 4cccafa)
- scripts/sync_app_bundle.sh: cp target/release/meetily -> 言镜 AI.app/Contents/MacOS/言镜 AI, 验证 hash 一致
- frontend/src-tauri/build.rs: macOS 下输出 reminder '跑 ./scripts/sync_app_bundle.sh'
- guard 升级: 75 → 76 anchor (+93_sync_app_bundle_script)
- §93 文档加 §93.1 章节

### 新工作流 (必走)
1. cd frontend && cargo build --release
2. ./scripts/sync_app_bundle.sh  ← 必跑
3. open '/Users/wangwei/Documents/离线会记/target/release/言镜 AI.app'

### 验证
- sync 后 hash 一致 6bc06d8
- guard 76/76 PASS
- .app bundle 启动测试 OK (PID 9158)
