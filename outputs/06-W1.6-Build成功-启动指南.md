---
tags: [W1.6, build, 成功, 启动, 离线会记]
created: 2026-07-08
status: ✅ cargo check 通过,等待你跑 tauri dev 看到 GUI
---

# 离线会记 W1.6 — Build 成功,启动指南

## 🎉 全部通过

| 步骤 | 状态 |
|---|---|
| 装 Rust 1.96.1 | ✅ |
| 装 cmake 4.3.4 | ✅ |
| 装 rustfmt 1.9.0 | ✅ |
| 装 Xcode 26.6 | ✅(你装的) |
| cargo check(90% 编译) | ✅ |
| 编译 llama-helper sidecar (1m 11s) | ✅ |
| 复制 llama-helper 到 binaries/ | ✅ |
| 重跑 cargo check | ✅ 11.31s,exit 0 |
| pnpm install (46s) | ✅ |
| 启动 tauri dev | ⏳ 等你跑(GUI 需窗口) |

## 启动命令(你本机 1 条命令)

```bash
. "$HOME/.cargo/env"
cd ~/Documents/meetily/frontend
pnpm tauri:dev
```

**首次完整启动**:
- Next.js dev server 拉起(port 3118)
- Tauri 编译 + 启动 GUI 窗口
- 窗口标题:**离线会记 — 本地 AI 会议转录**
- 总耗时:2-10 分钟(首次;之后秒开)

## 启动后验证 5 件事

1. **窗口标题**:`离线会记 — 本地 AI 会议转录`(不是 "meetily")
2. **About 页面**:打开 app → 菜单 → About → identifier 应是 `cn.lixianhuiji.app`
3. **设置 → LLM Provider**:**只剩 Ollama**(OpenAI/Claude/Groq/OpenRouter 不可选)
4. **断网测试**:关 WiFi → 录音 30 秒 → 转录 → 摘要(全程不报错)
5. **抓包验证零外网**:
   ```bash
   sudo tcpdump -i lo0 -A | grep -E "posthog|ollama.ai|api\.anthropic|api\.openai|groq|openrouter"
   ```
   期望:**零匹配**

## 已知小问题(不影响功能)

- `cargo check` 有 7 个 warning,都是未使用函数(Meetily 上游就有)
- 启动时控制台可能有"FFmpeg download"日志(已下好,二次启动秒过)

## 跑出错了贴什么给我

```bash
. "$HOME/.cargo/env"
cd ~/Documents/meetily/frontend
pnpm tauri:dev 2>&1 | tee /tmp/tauri-dev.log
```

启动后**卡在哪一步、报什么错**贴给我。

## 启动成功后做什么

1. ✅ 截图 1 张(给 W1 报告留档)
2. ✅ 跑 1 次录音(30 秒)→ 看转录
3. ✅ 跑 1 次摘要
4. ✅ 关 app,贴日志给我

## 关键文件位置(回顾)

| 用途 | 路径 |
|---|---|
| W1 fork | https://github.com/samwang0420-code/meetily/tree/feature/w1-no-cloud |
| W1 commit | https://github.com/samwang0420-code/meetily/commit/280e9a6 |
| 本地 fork | `~/Documents/meetily/` |
| W1 改造文档 | `~/Documents/Obsidian Vault/项目/N-离线会记/04-W1-推送完成-已在fork.md` |
| 启动日志 | `/tmp/tauri-dev.log` |

## 启动后下一步

W2 我准备出 **sherpa-onnx + SenseVoiceSmall 国产 ASR 集成**(commit 叠加到 `feature/w1-no-cloud`):
- 新增 `audio/transcription/sherpa_provider.rs`(实现 `TranscriptionProvider` trait)
- `audio/transcription/mod.rs` 注册新 provider
- 默认中文模型切换到 SenseVoiceSmall(40M/234MB)
- 模型下载脚本
- 中文中英混合标点优化

你只需 `git pull` 即可拿到。
