# §78 P0-A 跨会议知识图谱 Phase 1 (2026-08-06)

## 触发
71 报告 P0-A 战略级: Charoite 标杆 + 唯一让言镜 AI 进"长期付费"漏斗的功能。

## Phase 1 交付（本 turn）
- **Migration `20260806000001_topic_graph.sql`**：4 张表
  - `topic_node` (canonical_name UNIQUE 去重) + 2 索引
  - `meeting_episode_node` (UNIQUE(topic_id, meeting_id) 幂等) + 2 索引
  - `relates_to` (4 种 relation: related/causes/supersedes/contradicts)
  - `topic_dossier` (status / summary / open_questions / last_decided)
- **新模块** `frontend/src-tauri/src/topic_graph/{mod,extract}.rs` (~390 行 + 6 单测)
  - `normalize_topic_name()` 解决 "API 限流" == "API限流" == "API 限流!" 归一
  - `upsert_topic()` 冲突时自动 +mention_count + last_touched_at
  - `link_meeting()` UNIQUE 约束保证幂等
  - `search_topics()` LIKE 模糊，返 sample_excerpts 前 3
  - `get_topic_dossier()` 完整 dossier + 所有 episodes
  - `ExtractPromptBuilder` 纯函数 LLM prompt 构造
  - `parse_extract_response` 逐行 JSON 解析，跳过无效行
- **2 个 Tauri command** 在 lib.rs 注册
  - `api_topic_search(query, limit)` 
  - `api_topic_get_dossier(topic_id)`

## §37 闸门
- ✅ cargo check 0 errors
- ✅ cargo test topic_graph **6/6 PASS**
- ✅ binary 启动自动迁移 4 张表
- ✅ check_historical_fixes.py 13/13 PASS
- binary 69M mtime 8/6 23:59

## Phase 2 路线图（5-10 天）
1. **service.rs 摘要完成时 spawn extract_and_link** — Phase 1 trigger_after_summary 旁加
2. **BuiltInAI (Qwen 3.5 2B) 实际调 LLM 提取** — 用 ExtractPromptBuilder.build(summary) → LLM.complete → parse_extract_response
3. **topic_dossier 夜间增量 rebuild** (0-6 点跑未处理 topic) — 接 P2-B
4. **会议开始时弹"⏮ 7/15 讨论过 Topic X, 当时状态 Y"** — 会议记录页签前面查 api_topic_search

## 关联
- 71 报告 P0-A
- §37 闸门
- §P0-B Obsidian (wikilink 接 P0-A dossier)
- §P1-A MCP server (加 api_topic_search tool → v0.2.0)
