# 2026-07-29 v0.7.0-rc5 ASR fallback 硬化 + i18n 完整化

## ASR 静默 fallback 硬化

**问题**: `frontend/src-tauri/scripts/sherpa_asr.py:330 _ensure_model` 在请求模型不存在时静默 fallback 到任意已装模型 (funasr-nano), 引发 0 段识别.

**修复 (commit e852148)**:
- `models.get(tag) == None` 时: stderr WARN + `raise ValueError(msg)`
- 调用方 `stream_chunk` 捕到 → 返 `{ok:False, error:'model load failed: ...'}`
- 前端可见真实错误

**测试结果** (`/tmp/test_fallback3.py`):
```
✓ funasr-nano-zh / paraformer-zh / paraformer-zh-int8 alias → 正常 load
✗ sense-voice-zh-int8 / sensevoice-zh / parakeet / whisper / unknown → 硬错误
```

## i18n 完整化

- zh=850 / en=852 paths / t()=576 调用 / missing=2 (都是动态模板 `summary.phase.${X}`)
- 49 zh + 49 en 个新 key 已补齐
- en.ts 结构性 syntax bug 修复

## macOS 文件系统缓存陷阱 (新发现!)

- Python in-place write 后 bash 看到 stale 副本 (md5sum 报 52315 bytes vs Python 52412 bytes)
- 解决: `mv /tmp/new_file.py <target>` 而不是 in-place write

## 跨会话防漏 (AGENTS.md §30)

- 任何用户决策 → 三处同日落: Obsidian 决策日志 + AGENTS.md §X + 代码常量注释
- 守门脚本 `scripts/check_user_decisions.{py,sh}` 跑: 业务常量 / i18n / 死代码
- 用户必做 (§15): 重启 app + 录 30s + DB 段数 ≥ 1 (无法 CLI 测)

## 关联 commit

- `e852148` fix(asr) 静默 fallback 硬化
- `cd460d8` fix(asr+i18n) 双写 (submodule sync only, 实际 i18n 改动在 fcc821f rc4 已提交)
