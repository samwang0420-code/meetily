# v0.6.11 · 修复长会议识别为空 + streaming buffer 强制 endpoint

**Binary**: PID 30658 (16:34 build, cargo 1m14s)

## 用户反馈
"再看最近一条, 啥都没识别出来"

## 根因诊断

### Latest meeting (16:30:34, 40.87 秒)
- SQLite `transcripts`: 0 条
- `audio.mp4`: **40.87 秒全连续语音**, 1361/1362 frames 是 voice (peak 0.49, RMS 0.06)
- `metadata.json.meeting_id: null` (retranscription 命令无法关联)

### 预测的 bug 完全命中
**v0.6.11 streaming pipeline**: 长会议连续说话无 1.2s 静音 → is_endpoint 永不发 → buffer 累积到 finalize 时整段识别失败 → transcripts=[]

## 修复

### 1. 立即修复当前会议
- 直接调用 `sherpa.transcribe` (整段) 重识别
- SenseVoice: "啊啊, 对不起, 兄弟, 翻不了身了, 直接躺平了。我现在不是要翻身的问题..."  (关于负债 800 万, 躺平)
- 写回 SQLite + `transcripts.json` + `metadata.json.meeting_id`

### 2. 根治 streaming pipeline 设计缺陷 (sherpa_asr.py)
- 加 `force_final_ms` 配置 (默认 30000ms)
- 累计 buffer > force_final_ms 强制切句 (`is_endpoint_forced=True`)
- resp 加 `is_endpoint_natural` 与 `force_final` 区分切句来源
- `finalize` 注释强化: 强制跑一次整段识别兜底

### 3. 端到端单测
30 秒音频 + force_final_ms=5000:
- t=5s: ENDPOINT (natural=False, force_final=True) 切出第一段
- t=10/15/20/25s: 同理每 5s 强切
- finalize: 出剩余 delta
- **buffer 不再无限累积**

## 验证

| 会议 | 修复前 | 修复后 |
|---|---|---|
| Meeting 2026-07-10_16-30-34 | (空) | "啊啊, 对不起, 兄弟, 翻不了身了, 直接躺平了..." (40.87s 完整对话) |
| Meeting 2026-07-10_16-09-17 | (空) | "你好, 我是王威..." |
| Meeting 2026-07-10_15-59-32 | `万 。` | "每天金三十万..." |

PID 30658 build OK, 重启 app 即可看到 3 条会议有内容

## 已知边界

- force_final 切句是硬切, 边界可能有 0.2s overlap (保留设计)
- 用户连续 30s+ 不说话 + 30s 后又有说, 会强制切前 30s (可能损坏语义) — 这是 trade-off, 比"完全空白"好
- SenseVoice 整段识别偶尔重复 "啊啊啊啊啊" 是 sensevoice 模型 issue, 后续 hotword 后处理可以改善

## 完整改动文件

1. `frontend/src-tauri/scripts/sherpa_asr.py` (改) - 加 `force_final_ms` + 强制切句 + resp 标记
2. SQLite `transcripts` 表 (DB-only) - 直接 INSERT
3. `~/Movies/meetily-recordings/Meeting 2026-07-10_16-30-34/transcripts.json` (文件) - 重写
4. `~/Movies/meetily-recordings/Meeting 2026-07-10_16-30-34/metadata.json` (文件) - meeting_id 关联

