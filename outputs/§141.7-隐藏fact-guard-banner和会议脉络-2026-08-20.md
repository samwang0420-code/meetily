# §141.7 隐藏 fact_guard banner + 会议脉络页(2026-08-20)

## 触发

用户 8/20 反馈两张截图:
- 图 1: 红色"事实核对失败, 已用保守纪要替代 · 7 处需核对"banner,显示 "167万/30万/43万 数字未在原文出现"、"2016年/2016年11月/2018年9月 日期未在原文出现"
- 图 2: 会议脉络页(原"知识图谱")— 6 主题 / 2 决议 / 1 项目 / 2 人物,4 个主题卡片展示

用户原话:
> 这个功能去掉吧,保留上面的 D 方案就行了。会议脉络看不懂,你可能自己都不知道自己在做什么。

## 决策

按 §104 / §106 用户偏好"隐藏 ≠ 删除"(便于恢复), §18 不主动删 i18n keys:

### 1. 隐藏 FactGuardBanner
**D 方案(黄底高亮意外事实)保留** — 用户明确"保留上面的 D 方案"
**Banner 红框去掉** — 用户判定"华而不实"

### 2. 隐藏会议脉络
- Sidebar "会议脉络"入口: `{false && ...}` 守卫,代码保留
- `/knowledge` 路由: 文件顶部加 `KNOWLEDGE_DISABLED = true`,early return 简洁占位页
- 数据(6 主题 / 2 决议 / 1 项目 / 2 人物)保留在 DB,未删除
- 用户后续如需恢复,改 KNOWLEDGE_DISABLED 为 false 即可

## 改动文件(5 个, +45 / -8)

| 文件 | 行 | 改动 |
|---|---|---|
| `frontend/src/components/AISummary/BlockNoteSummaryView.tsx` | 277, 297 | 2 处 `<FactGuardBanner .../>` → 注释 `{/* §141.7: 隐藏 banner */}` |
| `frontend/src/components/Sidebar/index.tsx` | 782 | knowledge 按钮用 `{false && (() => {...})()}` 守卫 |
| `frontend/src/app/knowledge/page.tsx` | 63, 178-216 | 加 `const KNOWLEDGE_DISABLED = true`,return 前 early return 简洁占位页 |
| `scripts/check_historical_fixes.py` | 1516-1525 | 加 4 个 §141.7 anchor |

## §37 硬闸门

| 项 | 结果 |
|---|---|
| tsc --noEmit | 2 个 §18 bun:test 已知错误(不动) |
| next build | prerender 失败是 b7f4725 HEAD 已有的(刚 stash 验证)— §18 不动 |
| cargo check --lib | 0 errors, 38 §18 warnings |
| cargo build --release | 1m 58s, binary 73M mtime 8/20 11:35, sha=18c3f4b3ebe5 |
| check_historical_fixes.py | **434/434 PASS** (430 → 434, +4 §141.7 anchor) |
| sync_app_bundle.sh | §108 sidecar OK + §99.6 tauri bundle OK + §98 codesign OK |

## §15 GUI 验收(用户必做)

```
killall meetily 2>/dev/null
open '/Users/wangwei/Documents/离线会记/target/release/言镜 AI.app'
```

期望:
1. 打开任一历史摘要 → 不再显示红色"事实核对失败"banner;意外事实仍以黄色背景高亮(D 方案)
2. Sidebar → "会议脉络" 入口消失(只剩"工作台/录音/搜索/设置")
3. 直接访问 /knowledge → 简洁占位页"会议脉络已停用",无 6 主题/2 决议/1 项目/2 人物 等复杂 UI

## 隐藏 ≠ 删除 — 恢复方法

- Banner: `BlockNoteSummaryView.tsx:277,297` 把 `{/* §141.7: 隐藏 banner */}` 改回 `<FactGuardBanner .../>` 即可
- Sidebar: 把 `{false && (() => {...` 改回 `{(() => {...`
- /knowledge: `page.tsx:63` 把 `const KNOWLEDGE_DISABLED = true` 改 false,i18n 0 新增

## 关联

- §141 (B+D 方案 VERBATIM FACT-CHECK)— D 方案保留
- §104 (Sidebar 改名 + 4 面板隐藏)— 同用户偏好"隐藏不删"
- §106 (模型设置 Modal 切换功能删除)— 同 §18 精神
- §18 (不主动删 i18n keys / 不主动改无关 bug)
