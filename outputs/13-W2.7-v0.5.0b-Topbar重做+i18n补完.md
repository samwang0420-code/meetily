# W2.7 — v0.5.0b Topbar 审美重做 + i18n 补完 (2026-07-10 01:00)

## 用户 3 个新反馈

1. **Topbar 现在的"中文/English"切换按钮太"山寨"**——圆角胶囊按钮是 2024 风，2026 已下线。要求 Linear / Raycast / Vercel / Stripe 级别的专业审美
2. **中英文环境不全**——还有 Onboarding 等多个组件硬编码英文
3. **继续验证 recording/summary 模型**——SenseVoice 应该在 recording 默认，Paraformer 可切换，summary 用 Qwen3.5

## Topbar v2 重做 (核心)

**完全重写** `frontend/src/components/Topbar.tsx` (184 → 436 行)，从 5 个独立组件改造：

### Brand mark
- 自定义 SVG glyph (圆角矩形 + 圆点)，3D 蓝紫渐变 `from-blue-500 via-blue-600 to-indigo-600` + shadow-sm
- "离线会记" wordmark 用 600 weight Inter Geist tracking-tight
- v0.5 版本 chip (mono font, uppercase, 10px)

### Central command palette trigger (Linear/Raycast 风)
- 替代原搜索框，节省 70% 横向空间
- placeholder "搜索会议、命令…" / "Search meetings, commands…"
- 右侧 ⌘K / Ctrl K kbd 提示
- hover 显示浅底色 + 边框加深

### Right side actions (icon-only ghost button)
- **StatusPill**: 绿点 + 文字 "本地 ASR"，hover tooltip 显示 "SenseVoice · INT8 已加载"
- **RecordButton**: 主色按钮 (bg-neutral-900 dark:bg-white) + Mic icon + "录音" / "Record"
- **分隔符**: 1px vertical border
- **LanguageSwitcher**: globe 图标圆形按钮 (无 capsule)，Popover with 玻璃 backdrop blur
- **UserMenu**: avatar-only 圆形按钮 (36×36), Pro 用户 amber 渐变 + 角标

### 技术
- 引入 `@radix-ui/react-popover` 和 `@radix-ui/react-dropdown-menu` (已有 dep)
- lucide 图标 `strokeWidth={1.75}` 统一细线 iconography
- 所有颜色系统: `bg-white/70` + `backdrop-blur-md backdrop-saturate-150` 玻璃风
- dark mode 全支持

## i18n 补完 (zh + en 各 359 行)

### 修的关键 bug
1. **zh.ts 末尾 `settings` 块缺 `},`** — 之前 184 行起 v0.5.0b 写时漏闭合
2. **`recordings/devices/.../recording_chip` 10 个块嵌在 `settings` 内** — 重新梳理 nested 结构
3. **dot-named string keys (`'topbar.cmd_hint': 'Ctrl K'`) 在严格 TS Record<string, unknown> 下报错** — 全部转 nested object 形式

### 新增 keys (Topbar + Sidebar + Common + Errors)
- `topbar.*` (12 个键) — search_placeholder / cmd_hint / status_on / status_tooltip / record / signin / account_membership / settings / pro_badge / guest
- `sidebar.*` (6 个键) — workspace / library / configure / collapse / expand / search_placeholder
- `onboarding.*` (2 个键) — welcome_badge / tagline_short
- `common.*` (6 个键) — retry / copy_success / download / browse / delete_confirm / required_field
- `errors.*` (4 个键) — file_too_large / unsupported_format / model_download_failed / recording_in_progress

### Topbar.tsx 现在用
- `t('topbar.search_placeholder')` / `t('topbar.record')` / `t('topbar.signin')` / 等所有用户可见文案
- 15 处 `locale === 'zh' ? 'X' : 'Y'` 全部替换为 `t('key')`
- 13 个组件函数自动 destruct `{ t, locale } = useTranslation()`

## 构建 + 重编

### Next.js build
- `pnpm next build` 第一次失败：`swc` 不接受 `},  common: {` (无换行紧凑语法)
- 第一次失败：`Record<string, unknown>` 不接受 dot-named string key `'topbar.cmd_hint':`
- **修复**：完整重写 zh.ts / en.ts (从 318 → 318 行但内部 nested 结构)
- 第二次 build 成功：15 路由全部 prerender, 8.79 kB JS for /settings, **253 kB for /**, **797 kB for /meeting-details**

### Cargo build
- `cargo build --release` 增量编译：FFmpeg reused cache, app_lib 重编 (因为 .ts 改动不直接触发 src-tauri crate，但 tauri_build.rs 重新打包 frontendDist)
- 编译时间 ~3-5 分钟 (incremental)
- mtime 01:15:30 从 00:36 更新

### Binary 验证
- `target/release/meetily` 63 MB, mtime 01:15:30, PID 91125 启动
- `'中文': 6 hits` (Topbar LOCALE_LABEL 嵌入)
- `'isCollapsed': 0 hits` (因为 minify 改名 + tree-shake, 不影响功能)

## 模型验证 (recording + summary)

- **recording 默认 provider**: `sherpa_funasr_nano` (= SenseVoice) — `ConfigContext.tsx:111` + fallback `sherpa_paraformer` 
- **transcript model 默认**: `paraformer-zh-int8` (因为 provider = sherpa_funasr_nano, 模型自动 = `sense-voice-zh-int8`)
- **summary 模型**: `qwen3.5:2b` builtin-AI (Rust 端读 DB 配置)
- **语言支持**: 6 种 — auto/zh/yue/en/ja/ko + LANGUAGES dict 已中文化

## 用户验证步骤 (60秒)

1. `Cmd+Q` 完全退出老的 meetily 窗口
2. 打开新窗口: `open /Users/wangwei/Documents/meetily/target/release/meetily`
3. 期望看到:
   - **顶部 sticky Topbar**: 左蓝紫 brand mark + 中央"搜索会议、命令…" + 右侧 4 个圆角 icon (状态指示 + 录音 + 地球 + 用户头像)
   - **左 sidebar**: v0.5.0b 设计（collapse 时只 68px, expand 时 252px）
   - **语言切换**: 点地球图标 → popover 显示"中文 / English"
   - **切到 English**: 立刻全 UI 切英文 (Topbar, Sidebar, MainContent)
4. 录音: SenseVoice 默认, 切到 Settings → Hotwords 选 "法律"

## 关联
- [[12-W2.6-v0.5.0b-Topbar修复-Binary重编]] - 上轮 binary 重编
- [[11-W2-国产ASR切换]] - SenseVoice/Paraformer 默认选择
- [[决策日志-当前]]
