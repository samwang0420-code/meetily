---
tags: [W1.8, 修复, ASR, Whisper, Parakeet, Ollama, 中文]
created: 2026-07-08
status: ✅ 修复链完成,等用户重试
---

# 离线会记 W1.8 — ASR 主链路修复

## 根因(3 个连环问题)

| # | 问题 | 现象 | 修复 |
|---|---|---|---|
| 1 | **HuggingFace 国内访问失败** | meetily 试着下 Whisper 模型但连不上 huggingface.co,截图显示进度但实际 0 byte | 手动从 hf-mirror.com 下 `ggml-small.bin` 487MB 到 `~/Library/Application Support/cn.lixianhuiji.app/models/` |
| 2 | **默认 ASR 是 Parakeet(英文模型)** | onboarding.rs:194 写死 `provider=parakeet`,Parakeet 不支持中文,录中文 100% 失败 | SQL 改 `transcript_settings` 表: `provider=localWhisper, model=small` |
| 3 | **Ollama 未装 + 模型不对** | 摘要跑不了(需要 ollama serve + qwen2.5) | brew 装 ollama 0.31.1,拉 qwen2.5:1.5b 986MB |

## 现在状态(全 OK)

- ✅ Rust 1.96.1 + cmake + rustfmt + Xcode 26.6 + llama-helper 编译
- ✅ Whisper ggml-small.bin (487MB) 装在 `~/Library/Application Support/cn.lixianhuiji.app/models/`
- ✅ Ollama 0.31.1 跑在 :11434(daemon)
- ✅ qwen2.5:1.5b (986MB) 已拉
- ✅ tauri dev 启动(meetily GUI 已拉起)
- ✅ 数据库 settings 表已改为 localWhisper + qwen2.5:1.5b

## 你要做的(0 步)

**meetily 已经在跑**!直接去测试:

1. 打开 meetily 窗口(应该已经在了)
2. 点"开始录音"→ 录 30 秒中文
3. 看转录结果(应该出 Whisper small 的中文)
4. 跑一次摘要(应该走 Ollama qwen2.5:1.5b)
5. 截图给我

## 已知不足(W2 解决)

- ⚠️ **Whisper small 中文准度一般**(比 large-v3 差些,但能跑)
  - W2 计划:sherpa-onnx + SenseVoiceSmall 替换(中文 SOTA)
- ⚠️ **Ollama 启动方式不持久**:当前用 `nohup` 跑,重启 Mac 后要重启
  - 修复:`brew services start ollama` 开机自启
- ⚠️ **首次启动慢**:Whisper small 加载要 5-10 秒
  - 正常,无修复必要

## W2 计划(等 W1.8 验证后)

如果当前 Whisper small 中文效果还行,W2 优先做商业化骨架:
- 离线授权系统(机器指纹 + 激活码)
- 强制录音告知弹窗(法律合规)
- 行业术语插件(法律/医疗/金融 3 套,各 ¥99-149)
- 闲鱼/淘宝/微店收款页

如果中文效果拉胯,W2 立刻出 sherpa-onnx 替换:
- 预计中文 CER 从 25%(Whisper small) → 8%(SenseVoice)
- 速度从 1x → 5x(234MB 小模型,CPU 实时)

## 关键文件位置

| 用途 | 路径 |
|---|---|
| Whisper 模型 | `~/Library/Application Support/cn.lixianhuiji.app/models/ggml-small.bin` |
| SQLite 配置 | `~/Library/Application Support/cn.lixianhuiji.app/meeting_minutes.sqlite` |
| Ollama 二进制 | `/opt/homebrew/opt/ollama/bin/ollama` |
| Ollama 模型 | `ollama list` 看 qwen2.5:1.5b |
| meetily 进程 | PID 在 `ps aux | grep meetily` |
| 启动日志 | `/tmp/tauri-dev4.log` |

## 现在的关键命令

```bash
# 看 ollama 是否跑
curl -sL http://localhost:11434/api/version
# → {"version":"0.31.1"}

# 看本地模型
ollama list
# → qwen2.5:1.5b

# 测 ollama 推理
curl -X POST http://localhost:11434/api/generate -d '{
  "model":"qwen2.5:1.5b",
  "prompt":"你好,用一句话介绍离线会记",
  "stream":false
}'

# 看 meetily 实时日志
tail -f /tmp/tauri-dev4.log
```

## 网络说明

**huggingface.co 国内访问失败**(CGNAT/GFW 拦截),用 `hf-mirror.com` 镜像绕过。W2 我会在 meetily 源码里加 mirror fallback,以后开箱即用。

**ollama.com 国内访问也慢**,从 GitHub release 装更稳。
