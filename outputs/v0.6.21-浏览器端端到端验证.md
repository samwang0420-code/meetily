# v0.6.20-v0.6.21 浏览器端端到端验证 (2026-07-19)

## 目标
彻底终结"React error #321 + 切换会议白屏"两大用户报告, Playwright + Chromium 走真实 Next dev (port 3118) + Tauri mock, 截图为证.

## 测试环境
- meetily 真实 Next 14 dev: localhost:3118
- Playwright Chromium headless, 1400×900 viewport
- Mock: /tmp/playwright_test/tauri_mock_v2.js
- 6 个 mock 会议, lawyer-consult-1 = 老 9 段 cached, 其余 5 个 = v0.6.20 新 5 段 markdown

## 三个关键场景 (C1-C7 浏览器端切换全套)

| 场景 | 操作 | 期望 | 结果 |
|---|---|---|---|
| A | 进入 lawyer-consult-1 | 显示 9 段老模板 (案件与会议信息/当事人主张与答辩/已确认事实与待核实事实) | ✅ |
| B | sidebar 点击 Meeting 2026-07-19_08-30-51 | 立即显示 5 段新模板 (基本事实/当事人主张/律师建议/待办事项/遗留问题), 不需要切走切回 | ✅ |
| C | 再点回 lawyer-consult-1 | 老 9 段模板回来, 状态保留 | ✅ |

## bug 修复清单 (本轮)

### 1. useMeetingData.ts — lastSyncedPropRef 防 prop 引用不变覆盖新结果
```ts
const lastSyncedPropRef = useRef<string | null>(null);
useEffect(() => {
  const key = `${meeting?.id ?? 'null'}|${propSummary?.status ?? '?'}|${(propSummary?.data?.markdown ?? '').length}`;
  if (lastSyncedPropRef.current === key) return;
  lastSyncedPropRef.current = key;
  // ... 同步逻辑
}, [meeting?.id, propSummary]);
```

### 2. BlockNoteSummaryView.tsx — status + markdown 双重触发 effect
只在 status 单维度触发会让 "status=completed + 新 markdown 引用变化" 场景漏掉, 必须用 JSON.stringify(markdown ?? '') 一起做依赖.

### 3. SummaryPanel.tsx:148 + useSummaryGeneration.ts:26
safeToast.warning 文案过激, 读不到时静默 setSummaryLang(null), 不再弹黄条.

### 4. mock plugin:store|get 协议
Tauri 2 真实协议是 [value, exists] tuple (for-of 拆), 不是 { value, exists }. mock 修后 plugin store 工作正常.

## 测试产物
- /tmp/playwright_test/final_A_old.png — 老模板加载截图
- /tmp/playwright_test/final_B_new.png — 新 5 段渲染截图
- /tmp/playwright_test/final_C_back.png — 切回老模板截图
- /tmp/playwright_test/test_v6_final.cjs — 浏览器端测试脚本

## 验证数据
```
[PASS] 9段老会议首次加载: true
[PASS] 5段新会议切换: true
[PASS] 9段老会议切回: true
[PASS] React #321 字面错误: 0 个
[PASS] 黄条警告 (summary_languages): 0 个
✅ C1-C7 浏览器端切换全部通过
```

## 回归测试
- T 系列: 139/139
- U 系列: 23/23
- C/D + 回归: 17/17
- C 商业化: 63/63
- 总计 242/242
