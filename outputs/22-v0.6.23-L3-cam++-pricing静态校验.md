# v0.6.23 L3 后处理 + cam++ 下载容错 + pricing 静态校验

## 本轮完成

- **B｜D5 L3 同音后处理（Rust）**  
  `frontend/src-tauri/src/audio/industry_terms.rs` 新增 `safe_hotword_replace`，零依赖实现：
  - Damerau-Levenshtein 距离 + 同 kind run 扫描
  - 0.78 相似度阈值 + 词长 3-12 字符 + 标点/语种边界保护
  - 默认关闭（`correct_industry_terms` 不传 known_terms 不会触发 L3），保留 `离线会议` 不被误改
  - 8 个单元测试全部通过，含 5 类误伤保护

- **C｜pricing 静态校验**  
  `frontend/src/app/pricing/page.tsx` 重写：
  - 三档卡（匿名/免费/Pro）+ 流程 + FAQ + 隐私 banner + 底栏全部走 i18n
  - 删除未引用的 `TIERS` 常量，避免代码与文案双源
  - 字典 715/715，中英完全对齐
  - 新增 `frontend/tests/lib/pricing-i18n.test.mjs`：3 个测试 100% 通过
    - 中英键 1:1
    - pricing 页面无可见硬编码中文
    - 26 个关键 tier/feature/FAQ/footer key 在两套字典都存在

- **D｜cam++ 下载容错（Python）**  
  `frontend/src-tauri/scripts/diar_download.py` 新增 + 接入 `diar.py`：
  - 段/词双模型清单（pyannote-segmentation-3.0 + campplus-cn-en）
  - 双镜像（hf-mirror 优先 / huggingface 回退）
  - `.part` 临时文件 + HTTP Range 断点续传
  - 每次下载最多 3 次重试，最小字节数校验
  - per-model 线程锁避免重复下载
  - 失败返回结构化 `{"ok": false, "error": "..."}`，主流程不抛错
  - 5 个 Python 协议测试通过，离线安全

## 自测结果

- `cargo test --lib audio::industry_terms::` — **8/8 PASS**
- `python3 tests/diar_download_protocol.py` — **5/5 PASS** (offline-safe)
- `node --test tests/lib/pricing-i18n.test.mjs` — **3/3 PASS**
- `npx next build` — **19/19 页面生成成功**

## 边界

- L3 是 opt-in，调用方需主动传入 `known_terms`，默认行为不变
- cam++ 下载仅在 `is_available()` 返回 false 时才触发，主流程 ASR 不阻塞
- pricing 校验只覆盖可见 JSX 文本和字典键，不验证布局
- 没改后端 `worker.rs` / `engine.rs`，无需 §15 双验证
