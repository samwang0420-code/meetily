# §137.2 open_meeting_folder SQL 漏 template_id 字段修复 (2026-08-19)

**触发**: 用户 8/19 12:00 反馈"点击打开录音文件夹报错: Database error: no column found for name: template_id"。

**根因**: §123 commit 在 `MeetingModel` struct 加了 `pub template_id: Option<String>` 字段,但 `api/api.rs:1083` `open_meeting_folder` 的 `sqlx::query_as` 调用只 SELECT 5 个旧字段 (`id, title, created_at, updated_at, folder_path`),漏了 `template_id`。
sqlx derive `FromRow` 行为: 严格按 struct 字段从 row 取值, 缺字段直接报"no column found for name: template_id"。

**其他 MeetingModel 使用点** (4 处,扫描全过):
- `meeting.rs:12` — `SELECT *` (无问题, 包含所有列)
- `meeting.rs:65` — `SELECT id, title, created_at, updated_at, folder_path, template_id` (OK)
- `meeting.rs:128` — 同上 (OK)
- `api/api.rs:343` — 走 `MeetingsRepository::get_meetings` (内部 SELECT *)
- `api/api.rs:1083` — **唯一漏处** (本次修复)

**修复** (1 行 SQL):
```rust
// 修前
"SELECT id, title, created_at, updated_at, folder_path FROM meetings WHERE id = ?"
// 修后
"SELECT id, title, created_at, updated_at, folder_path, template_id FROM meetings WHERE id = ?"
```

**2 个新守卫锚点** (guard 349 → 351):
- `137_2_open_meeting_folder_sql_template_id` — api.rs:1083 包含完整 SELECT 列表
- `137_2_meeting_model_template_id_field` — models.rs MeetingModel 保留 template_id 字段

**§37 6 步硬闸门**:
- ✅ cargo check --lib: 0 errors (36 §18 warnings 不动)
- ✅ cargo build --release: 1m35s
- ✅ tsc --noEmit: 1 个 §18 bun:test 不动
- ✅ guard **351/351 PASS** (349 → 351)
- ✅ sync_app_bundle.sh: tauri bundle synced
- ⏳ §15 GUI 验收 (用户必做)

**§15 GUI 验收 (用户必做, 不能 CLI 测)**:
```bash
killall meetily 2>/dev/null
open '/Users/wangwei/Documents/离线会记/target/release/言镜 AI.app'
# 1. 打开任一会议
# 2. 点 TranscriptButtonGroup "录音" 按钮 (open meeting folder)
# 3. 预期: Finder 打开会议录音文件夹, 不再报错
# 4. 任意会议都试一个, 验证不依赖 template_id 是否 NULL
```

**教训 (§56 强化)**:
1. **sqlx `query_as::<_, T>` 严格按 T 字段 SELECT** — 加新字段必须同步所有 SELECT
2. **不写"SELECT *" 是 sqlx 用法错误** — 应该 `query_as_unchecked!` 或 `query!` (compile-time) 或 `SELECT *`
3. **必须扫所有 FromRow struct 的 query_as** — 改 struct 后 grep 全仓库验证
4. **加字段 commit 必须同时加 anchor** — 本节加 2 个 §137.2 anchor 防未来回归
5. **AGENTS.md §X 描述 ≠ 代码 commit** — §123 加 template_id 时漏改了 api/api.rs:1083

**关联**:
- §123 (8/18 加 MeetingModel.template_id 字段)
- §137.1 (8/19 nav_guard 修复) — 同 commit
- §15 §37 §56 §92 — 必须跑硬闸门 + commit 描述匹配代码
- [[137.2-open_meeting_folder-SQL漏template_id修复]] (Obsidian)
