# §151 禁止并行多分支 — 同一时间只允许 1 个未合并 codex/* 分支

**触发**: 用户 8/21 质问"你为什么要同时存在两个分支呢"。8/20 当天我开了两个并行分支：
- `codex/remove-topic-recall-popup` (11:17:54 +0800, `1eb62b0 §144`)
- `codex/legal-summary-fix` (11:58:41 +0800, `0cdaace §145`)

reflog 显示当天 checkout 来回切换, §115 已立规则"合并后自动删除"但**未明文禁止并行开分支**。

---

## 铁律

1. **同一时间只允许 1 个未合并的 `codex/*` 分支存在** — 检 `git branch -a | grep 'codex/' | wc -l` 必须 ≤ 1（不含已合并未删 / cleanup-recommended-tag 的）
2. **新需求发现时**:
   - (a) 当前分支已 commit 但未合并 → **在当前分支继续加 commit**（优先）
   - (b) 当前分支已 push 等合并 → **等当前 PR 合并后再开新分支**
   - (c) 紧急独立主题（不阻塞当前 release）→ 开新分支前**必须显式告诉用户"我会再开一个分支"**
3. **违反处理**:
   - 立即把新分支 merge --no-ff 到当前分支（或反之）
   - 删除已合并的源分支（本地 + 远端）
   - 在 commit message 注明"按 §115 + §151 清理分支"
4. **新章节/新决策 SOP**:
   - 任何 commit message 含 "§X" → 必须当日同时落地: 代码 + Obsidian + AGENTS.md §X
   - §115 / §151 是合并/分支相关, 单独 check 一次

---

## 今日已落地清理 (2026-08-21)

```bash
# 在 codex/legal-summary-fix 分支上
git merge --no-ff codex/remove-topic-recall-popup \
  -m "merge(codex/remove-topic-recall-popup): §144 TopicRecallPopup 删除 — 合并到 legal-summary-fix (按 §115 规则清理分支)"
# → b43bae2 (Auto-merging scripts/check_historical_fixes.py)
git branch -d codex/remove-topic-recall-popup
git push origin --delete codex/remove-topic-recall-popup
git push origin codex/legal-summary-fix
# → 5d0a1c5..b43bae2
```

| 操作 | 结果 |
|---|---|
| merge --no-ff | ✅ b43bae2 (5 files, +70/-143) |
| 本地删除 | ✅ Deleted branch codex/remove-topic-recall-popup (was 1eb62b0) |
| 远端删除 | ✅ `To github.com:samwang0420-code/meetily.git - [deleted] codex/remove-topic-recall-popup` |
| 远端 push | ✅ 5d0a1c5..b43bae2 → codex/legal-summary-fix |

---

## 未来规避检查 (每次 commit 前必跑)

```bash
git branch -a | grep -E '^\s+codex/' | grep -v 'remotes/' | wc -l
# 期望输出 1 (或 0, 如果当前 checkout 在 main)
# 如果 ≥ 2 → 停止 commit, 先按 §151 处理
```

**CI 自动化建议** (可选, 后续 §X 加):
```bash
# .git/hooks/pre-commit (本地)
#!/bin/bash
N=$(git branch -a | grep -E '^\s+codex/' | grep -v 'remotes/' | wc -l)
if [ "$N" -ge 2 ]; then
  echo "❌ §151 违规: 当前有 $N 个未合并 codex/* 分支, 必须先合并/删除"
  exit 1
fi
```

---

## §151 关联

- §115 (主分支发版 + 24h 自动清理, 8/13 立)
- §92 (决策迁移铁律, outputs + Obsidian + AGENTS.md 三处同步)
- §56 (AGENTS.md §X 描述 ≠ 代码 commit)

---

## 关联文档

- AGENTS.md §151 (本规则定义)
- [[120-完整1h47m推理实测13.83min-RTF-0.291-2026-08-13]] (注意: §120 编号已占用, 与本规则无关)
