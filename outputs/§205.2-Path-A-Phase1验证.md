# §205.2 Path A — XHToken llama.cpp fork Phase 1 验证 (2026-09-02 立)

**触发**: 用户拍板 §205.1 (commit 7c20c16) 集成后选 A — 让用户回电脑自己跑 Phase 1。

## 为什么 A

Phase 1 实测失败 (前 session):
- **B-α** (XHToken fork 编译): sandbox 拦 `git clone https://github.com/XHToken/llama.cpp.git`
- **B-γ** (Ollama 拉 Spark): Ollama 仓库无 spark-x2_5

用户回 Mac = sandbox 失效 = 可以 git clone + cmake build。

## 验证目标

用 XHToken fork 跑 Qwen3.5-2B-Q4_K_M, **对比 §197 baseline (7.44 tok/s)**:

| tok/s 结果 | 决策 |
|---|---|
| **> 7.44** | XHToken fork Q4_K 路径更优, 撤回 §197 llama-cpp-2 0.1.146 限制, 用 fork 编 Spark |
| **≤ 7.44** | §197 baseline 仍是 Apple Silicon Q4_K 最优, 走 B (MLX) 或 C (暂停 Spark) |

## 用户执行 (1 行)

```bash
bash outputs/§205.2-Path-A-Phase1验证脚本.sh
```

**预计耗时**:
- 首次: ~15 min (clone 5-10 min + cmake build 5-10 min + 2 次 tok/s 测试 2 min)
- 增量 (你已 clone,HEAD=a698f1cc3): 5-10 min (增量 build 1-2 min + 2 次 tok/s 测试 2 min)

## 智能检测 (放心 re-run)

脚本会智能跳过已完成的步骤:

| 检查 | 行为 |
|---|---|
| `/tmp/xhtoken-llama.cpp` 存在 | `cd` 进去,**不** git pull (保留你 clone 时的 HEAD) |
| `/tmp/xhtoken-llama.cpp/build/bin/llama-cli` 存在 | cmake **增量** rebuild (1-2 min) |
| 都不存在 | 首次 clone + 全量 build (15 min) |

你已 clone 成功 (HEAD `a698f1cc3` 含 spark2_5 auto parser merge commit),脚本会自动检测到。

## 脚本流程 (5 步)

| Step | 动作 | 输出 |
|---|---|---|
| 1 | 智能检测 clone (你已存在,直接 cd) | HEAD 行 |
| 2 | cmake 增量 rebuild (你首次 build 会先 cmake -B) | `build/bin/llama-cli` |
| 3 | `./build/bin/llama-cli --version` 验证 | Metal enabled 提示 |
| 4 | 跑 Qwen3.5-2B 200 tokens (中文 prompt 模拟真实摘要) | eval time 行 |
| 5 | 再跑 100 tokens 提取精确 tok/s, 跟 §197 baseline (7.44) 对比 | 数字 + 决策建议 |

## §205.2 已知边界 (按 §18)

- 25 cargo warnings 不动
- 1 fixture-bound test 不动
- 1 bun:test tsc error 不动
- §205.1 commit 已 push 但用户不能 GUI 加载 Spark (llama-cpp-2 0.1.146 不支持 spark2_5)
- 3.27GB Spark F16 GGUF 在 ModelScope, 用户需自量化或找 Q4_K_M 第三方版本

## 关联

- §205 (Phase 1 评估报告) / §205.1 (集成 commit 7c20c16)
- §197 (baseline 7.44 tok/s)
- §18 / §37 / §92 / §115 / §151 / §163 / §195
