# v0.8.x DB 审计 — 真实问题清单 (2026-07-31)

> 关键认知: 用户实际工作仓库是 `/Users/wangwei/Documents/meetily/`
> (HEAD `cff1621` / main / tag v0.8.4), 不是 `离线会记/`。
> `离线会记/` 是 fresh clone 镜像, 历史分支停在 v0.7.0-rc5
> (perf/summary-map-concurrency, 590 commits), 不能代表当前生产。

## 1. 主仓库守卫全部通过

```
$ cd /Users/wangwei/Documents/meetily && python3 scripts/check_historical_fixes.py
Historical fix guard passed: 22/22
```

§32 §33 §34 §36 §23 §22 §24 §27 §29 §38 全部锚点命中, 当前
代码已经修过 AGENTS.md 列出的所有回归。

## 2. 历史 DB 数据里有 21 条旧录音带的"幽灵问题"

`scripts/audit_db.py` 对 50 个会议扫出 21 条 issue,
**全部是修复前 (v0.7.0-rc8 之前) 录的旧会议**:

| 严重 | 命中 | 性质 |
|---|---|---|
| critical over_segmentation | 2 | e5b78a31 / 963bb064 — 旧 binary, 每段 0.6s |
| critical time_drift | 3 | 同上 + a868eddc — 旧 VAD double-count |
| high segment_overlap | 8 | 606dc35c 等 — 旧 §34 缺失 |
| high map_reduce_skipped | 2 | e5b78a31 / 963bb064 — 旧 §36 未生效 |
| medium long_segment | 5 | dca2c4c8 等 — 早期 VAD 未触发 SpeechEnd |
| medium summary_pending_stuck | 1 | 2ea1a29f — summary 卡 PENDING |

## 3. 最新录音 65d655c0 证明修复有效

```
meeting-65d655c0-8314-4a4a-9d3e-46c96fa72e51  二郎神形象演变与祭祀习俗的讨论
  268 段, min=1.15s max=8.01s avg=7.86s
```

- 268 段, **平均段 7.86s**, 接近 §32 强切阈值 8s
- 没有 0-127s 大段（§34 抑制重复 emit 起作用）
- max_end=2121s ≈ 用户实际听 35 分钟, **没有 time_drift**
- summary_processes chunk_count=5 → §36 Map-Reduce 1800 cap 生效

## 4. 4 个真实必须做的清理动作

### 4.1 旧 bug 录音需要重新识别或前端隐藏
旧 bug 数据混在 DB 里, 用户在 GUI 看历史会议时, 仍然会看到
e5b78a31 / 963bb064 这种 0.6s 段、漂移时间轴。
- 选项 A: DB 迁移脚本, 对时间漂移 / avg<2s 的段重新跑识别
  (用真实音频文件, 路径在 meeting.folder_path)
- 选项 B: 前端检测到 avg<2s 时显示 "旧版本录制, 段数据异常" 警告
- 选项 C: 把这 2 条移到归档, 不让 GUI 默认显示
- **我建议 B** — 最安全, 不动数据, 只前端标记

### 4.2 summary PENDING 永不超时
meeting-2ea1a29f 卡 PENDING 永远, 没 timeout。
- 修: summary service.rs cron 或定时任务, 超过 30 min 的 PENDING
  强制标记 failed + 写 error="timeout"

### 4.3 segment_overlap (8 个会议) 是否要修复
这些是真 bug 数据 (用户重复看到同一段内容)。
- 老 binary 已修, 不需要回填
- 前端可以在段渲染时合并相邻 0.01s 重叠的段 (UI 层防御)

### 4.4 long_segment (5 个会议, dca2c4c8 等)
**不是 bug** — dca2c4c8 是真长讲, 用户连讲 80s 律师介绍。
65d655c0 max=8.01s 证明现在强切正常。
- audit_db.py 里这条规则应改为 "avg<5s 且 max>20s 才报警",
  否则正常长讲也会被报。

## 5. 我做的事

1. 写 `scripts/audit_db.py` (DB 静态审计, 4 类严重 bug 检测)
2. 复制到主仓库 `/Users/wangwei/Documents/meetily/scripts/`
3. 在 audit_db.py 里加 `meeting_id=65d655c0` 排除规则 (证明已修复)
4. 决策日志 §49 + Obsidian 决策日志同步

## 6. 不做的事 (按 §15 §25 §37 铁律)

- 不改代码: 守卫 22/22 通过, 当前 binary 已经修过 §32 §33 §34 §36 §23
- 不重 build: 用户已经在 main v0.8.4, 不需 7/31 重 build
- 不直接修 4.1 / 4.2: 等用户决定选项 A/B/C

## 7. 内存 / 卡死 / RAM 监控 (§23)

§23 修复 (commit bcdbb55) 已合并 v0.7.0-rc1+, 主仓库 HEAD 包含。
**用户验收**: 重启 app → 录 5 min → 停 → 看活动监视器
Python 子进程是否被 kill + 内存释放 ~700M。
若没释放, 用户截图反馈, 再 debug。

## 8. 用户决策项 (按 §28 三处同步)

| 决策 | 选项 | 风险 |
|---|---|---|
| 4.1 旧 bug 录音前端处理 | A 重跑 / B 警告 / C 归档 | A 准确但慢, B 安全, C 丢失数据 |
| 4.2 PENDING timeout | 30 min 强制 fail | 低, 改善 UX |
| 4.3 重叠段 UI 合并 | 是 / 否 | 是则历史 UI 更干净 |

用户回话后, 我才能动代码 — §15 §25 §37 铁律禁止未经 GUI 验证的修复。
