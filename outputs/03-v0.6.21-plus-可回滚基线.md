# v0.6.21+ 工作区可回滚基线（2026-07-20）

## 结论

真实开发仓库 `/Users/wangwei/Documents/meetily` 中长期未提交的 v0.6.21 及后续 v0.6.23 工作区已完成整理，并建立 Git 提交、标签和离线备份三层恢复点。

## 基线标识

- 分支：`feature/w1-no-cloud`
- 提交：`cfd351a chore: establish v0.6.21-plus workspace baseline`
- 标签：`baseline-v0.6.21-plus-20260720`
- 提交规模：288 files changed，29,663 insertions，2,713 deletions
- 提交后状态：工作区干净

## 为什么叫 v0.6.21+

工作区不只是 v0.6.21，还已经包含 v0.6.23 相关商业化、i18n、cam++ 静态校验和诚实性修复文件。为避免伪造版本边界，本基线统一命名为 `v0.6.21+`，后续再单独统一正式产品版本号。

## 清理内容

只清理明确生成物和手工备份，没有改业务逻辑：

- 删除 Python `__pycache__` 和 `*.pyc`
- 删除 `worker.rs.bak`、`worker.rs.bak2`、`SettingsModal.tsx.bak`
- `.gitignore` 增加 `*.bak`、`*.bak[0-9]*`、`__pycache__/`、`*.py[cod]`
- 未纳入模型、音频、数据库、`.env`、密钥、`node_modules`、`target`、`.next`、`frontend/out`

## 离线备份

备份目录只留 Codex 工作端，不进 Obsidian：

`/Users/wangwei/Documents/离线会记/work/backups/v0.6.21-baseline-20260720-155921`

其中包含：

- `repository.bundle`：完整 Git 历史，已验证为 complete history
- `tracked-changes.patch`：原始已跟踪改动二进制补丁
- `untracked-files.tar.gz`：原始未跟踪文件
- `status.txt`、`diff-stat.txt`、`untracked-files.txt`

已在临时 clone 中验证：bundle 可 clone，补丁可对原始 HEAD 应用。

## 基线验证

### 通过

- `npm run build`：Next.js 19 个静态页面生产构建通过
- `cargo check`：通过，仅保留既有 17 个 warning
- Python：3/3 通过
- U 系列：23/23 通过
- C/D 系列：17/17 通过
- C 商业化：63/63 通过

### 已知基线失败

- 直接 `tsc --noEmit`：仅被 `tests/lib/blocknote-markdown.test.ts` 的 `bun:test` 类型缺失阻塞；Next production build 的类型检查通过
- T 系列：119/139，通过 119，失败 20
- 20 个失败主要是旧自测仍期待旧 Pricing 文案、匿名三档描述，以及 `useMeetingData` 的旧源码写法；现有实现和断言已漂移
- 按项目规则，本次只建立基线，不为清零测试而修改业务文案或无关实现

## 回滚方法

### 回到此基线

```bash
cd /Users/wangwei/Documents/meetily
git reset --hard baseline-v0.6.21-plus-20260720
```

### 保留当前工作，另开恢复分支

```bash
cd /Users/wangwei/Documents/meetily
git switch -c codex/recover-v0.6.21 baseline-v0.6.21-plus-20260720
```

### 仓库损坏时从 bundle 恢复

```bash
git clone /Users/wangwei/Documents/离线会记/work/backups/v0.6.21-baseline-20260720-155921/repository.bundle meetily-recovered
cd meetily-recovered
git switch feature/w1-no-cloud
```

## 下一步

可以从该基线安全开始：更多垂直模板、LLM 摘要 streaming、CoreML 加速路由、一小时说话人分离生产验证。每项应独立提交，避免再次堆成一个无法回滚的大工作区。
