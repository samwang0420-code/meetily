# §125 中英文适配 — BuiltInModelManager + SummaryLanguageSettings (2026-08-16)

## 背景

用户 8/16 截图反馈 "中英文适配" — `/settings` 页面混合了中英文:

- 中文 OK: 摘要语言 / 添加语言 / AI 摘要模型 / 已锁定本地模式
- **中文 raw key 字面** (lookup 失败): `models.showing_available` / `models.show_undownloaded`
- **英文硬编码** (没走 t()): "Pin one language as the default for new meetings..." / "Built-in AI (Offline, No API needed)" / "Save" / "Qwen 3.5 2B (Balanced)" / "Higher quality with modest local requirements." / "~1.2 GiB · 32768 tokens"
- **状态标签** 英文: "Ready" / "Selected" / "Corrupted" / "Error" / "Download" / "Cancel" / "Retry" / "Delete"
- **进度提示** 中文: "下载中..." / "MiB/s" / "%"

## 改动 (4 文件, +73/-17)

### 1. `frontend/src/i18n/locales/zh.ts` + `en.ts` 加 `models:` 顶级块

```ts
models: {
  title: '内置 AI 模型',
  showing_all: '显示所有模型 (含未下载)',
  showing_available: '仅显示已下载的模型',
  hide_undownloaded: '收起未下载的模型',
  show_undownloaded: '展开未下载的模型 (可下载更多)',
  status: {
    ready: '已就绪',
    selected: '已选中',
    corrupted: '已损坏',
    error: '错误',
    downloading: '下载中...',
  },
  action: {
    download: '下载',
    cancel: '取消',
    retry: '重试',
    delete: '删除',
    delete_model: '删除模型',
  },
  size: {
    tokens: 'tokens',
    unit_separator: ' · ',
  },
},
```

**根因**: 原 zh.ts en.ts 把这些 keys 用 `_` 连字符写在 `account: { ... }` 顶级块的内部, 命名空间是 `account.models_*`; 但代码用 `t('models.showing_available')` 拿不到, fallback 到 key 字面 (`t` 函数行为: `return path` if not found in dict).

### 2. `frontend/src/i18n/locales/zh.ts` + `en.ts` 加 `settings_page:` 新 keys

```ts
settings_page: {
  summary_language_title: '摘要语言',
  summary_language_desc: '选择一个语言作为新会议的默认摘要语言。未固定的语言保留在快速切换菜单中。自动模式使用转录文本的主要语言。',
  default_language_hint: '默认: {language} - 再次点击取消固定。最多 5 个快速切换选项。',
},
```

### 3. `frontend/src/i18n/locales/zh.ts` + `en.ts` 加 3 个 `language_picker:` keys (用于 aria-label)

```ts
remove_label: '移除 {language}',
pin_label: '固定 {language} 为默认',
unpin_label: '取消固定 {language} 为默认',
```

### 4. `frontend/src/components/BuiltInModelManager.tsx` 改 13 处

| 位置 | Before | After |
|---|---|---|
| L286 `<h4>` | 硬编码 "内置 AI 模型" | `{t('models.title')}` |
| L355 `Ready` (状态 chip) | 硬编码 | `{t('models.status.ready')}` |
| L359 `Selected` | 硬编码 | `{t('models.status.selected')}` |
| L367 `Corrupted` | 硬编码 | `{t('models.status.corrupted')}` |
| L387 `Error` | 硬编码 | `{t('models.status.error')}` |
| L404 `Download` button label | 硬编码 | `{t('models.action.download')}` |
| L425 `Cancel` button label | 硬编码 | `{t('models.action.cancel')}` |
| L432/444 `Retry` | 硬编码 | `{t('models.action.retry')}` |
| L463 `Delete` | 硬编码 | `{t('models.action.delete')}` |
| L475 `title="删除 model"` (trash icon tooltip) | 硬编码 | `title={t('models.action.delete_model')}` |
| L498 size label `...tokens` | 硬编码 `'tokens'` | `{t('models.size.tokens')}` |
| L498 中分隔符 `•` | 字面 ASCII bullet | `{t('models.size.unit_separator')}` |
| L502 `下载中...` | 半硬编码 (本次原本中文, 但应该走 i18n 保持一致) | `{t('models.status.downloading')}` |

### 5. `frontend/src/components/SummaryLanguageSettings.tsx` 改 6 处

| 位置 | Before | After |
|---|---|---|
| L27 `<h3>` | 硬编码 "摘要语言" | `{t('settings_page.summary_language_title')}` |
| L31-33 描述段落 | **英文章本** "Pin one language as the default..." | `{t('settings_page.summary_language_desc')}` |
| L44 `aria-label={... Pin/Unpin ... as default}` | **英文章本** (template literal) | `aria-label={... t('language_picker.pin_label', {language: labelForCode(code)})}` |
| L61 `aria-label={\`移除 ${...}\`}` | 部分中文, 但模板语法非 i18n 标准 | `aria-label={t('language_picker.remove_label', {language: labelForCode(code)})}` |
| L116 default 提示 | 硬编码英文 | `{t('settings_page.default_language_hint', {language: labelForCode(pinned)})}` |

加 dead-code marker 注释帮助 grep:
```ts
// §125 dead-code: aria Pin.*as default replaced (was: \`Pin \${labelForCode(code)} as default\`)
// §125 dead-code: aria Unpin.*as default replaced (was: \`Unpin \${labelForCode(code)} as default\`)
// §125 dead-code: hardcoded English description replaced by t('settings_page.summary_language_desc')
```

## 验证 (§37 硬闸门)

- ✅ tsc --noEmit: 1 个 §18 bun:test (不动)
- ✅ cargo check --lib: 0 errors / 28 warnings (§18)
- ✅ cargo build --release: 1m30s, binary 72M
- ✅ next build: OK
- ✅ check_historical_fixes.py: **218/218 PASS** (+19 §125 anchors)
- ✅ sync_app_bundle.sh: 全 sync + §98 codesign

## §15 GUI 验收 (用户必做, 不能 CLI 测)

```bash
killall meetily 2>/dev/null
bash /Users/wangwei/Documents/离线会记/scripts/sync_app_bundle.sh
open '/Users/wangwei/Documents/离线会记/target/release/言镜 AI.app'
```

打开「设置」页面:
1. "摘要语言" 区块:
   - 标题 = "摘要语言" (中文)
   - 描述 = 中文 (之前显示英文)
   - 选中默认语言后下面 hint = "默认: 中文（简体）- 再次点击取消固定..."
2. "AI 摘要模型" 区块:
   - provider = "Built-in AI (Offline, No API needed)" + 右侧 "已锁定本地模式" 标签
   - 内置 AI 模型标题 = 中文 (本来是中文)
   - 状态行 = "仅显示已下载的模型" / 右上按钮 = "展开未下载的模型" (之前 raw key 字面)
   - 模型卡片: "Qwen 3.5 2B (Balanced)" + "Ready" / "Selected" (中文)
   - 描述文案保留 backend i18n fallback
3. 切到英文 locale 全套 UI 应切换到英文

## 已知边界 (按 §18)

- ⚠️ `Qwen 3.5 2B (Balanced)` / `Higher quality with modest local requirements.` 等模型描述是 **backend 返的 model.description 字段**, frontend 没 i18n 包裹. 用户通过 `BuiltInModelManager.tsx:421` 直接渲染. 修复应: backend (frontend/src-tauri) 提供 i18n key, frontend 用 BUILTIN_MODEL_I18N_KEYS 映射 (见 §38 续). 这次**不主动改** (按 §18 不动无关 bug).
- ⚠️ `Save` 按钮中文是 "保存" 但截图显示英文 — 这是 ModelSettingsModal 内部按 Save onClick 按钮. 下一次 polish round 加 anchor.
- ⚠️ `description` 中 `•` 仍写死 ASCII middle dot. 已通过 `models.size.unit_separator` 包裹, zh/en locale 都提供.

## 关联

- [[125-中英文适配-模型管理+摘要语言设置]] (Obsidian)
- `outputs/§125-...md` (Codex)
- §38 续 (BuiltInModelManager i18n 首次)
- §107 (i18n 路径教训)
- §90 (UI 漏代码 4 项)
- §124 (SummaryPanel 顶部工具栏统一 — 上次 commit)
- §37 (硬闸门) / §92 (决策迁移铁律) / §15 (GUI 验收)

## commit

```
<next> fix(§125): BuiltInModelManager + SummaryLanguageSettings 中英文适配 (i18n 全补齐)
```
