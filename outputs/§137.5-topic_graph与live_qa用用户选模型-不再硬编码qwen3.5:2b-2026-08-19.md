# §137.5 topic_graph / live_qa 用用户选模型 — 不再硬编码 qwen3.5:2b

**触发**: 用户 8/19 原话
> model name qwen3.5-2b不要写死啊, 我们本地模型可以选择其他模型, 用户选择什么本地模型就要用什么本地模型

§137.3 (commit b961822) 修了 topic_graph 优先 BuiltInAI 兜底, 但**同时硬编码了 `"qwen3.5:2b"`** — 用户现在指出。

---

## 改动范围

### 1. `frontend/src-tauri/src/topic_graph/mod.rs`

**`trigger_after_summary`** — 加 2 个参数:
```rust
pub async fn trigger_after_summary<R: Runtime>(
    app: tauri::AppHandle<R>,
    pool: SqlitePool,
    meeting_id: String,
    summary_markdown: String,
    provider: LLMProvider,    // §137.5: 新增, 用用户选的 provider
    model_name: &str,         // §137.5: 新增, 用用户选的 model_name
) { ... }
```

**`preflight_llm_async`** — 接受 provider 参数:
```rust
pub async fn preflight_llm_async<R: Runtime>(
    app: &tauri::AppHandle<R>,
    provider: &LLMProvider,   // §137.5: 新增
) -> Result<(), String>      // §137.5: 从 Result<&'static str, String> 改成 Result<(), String>
```
- 内部 `match *provider { BuiltInAI => ..., Ollama => ..., _ => Err }`
- BuiltInAI 路径仍用 `builtin_ai_model_exists` helper
- Ollama 路径仍 3s ping `localhost:11434/api/tags`

**`extract_missing_topics`** — 加 2 个参数, 传给 `trigger_after_summary`:
```rust
pub async fn extract_missing_topics<R: Runtime>(
    app: tauri::AppHandle<R>,
    pool: &SqlitePool,
    max_meetings: i64,
    provider: LLMProvider,    // §137.5: 新增
    model_name: &str,         // §137.5: 新增
) -> Result<(usize, usize), String>
```

**`rebuild_topic_dossier`** — 加 2 个参数 (跨会议 dossier 重建):
```rust
pub async fn rebuild_topic_dossier<R: Runtime>(
    app: tauri::AppHandle<R>,
    pool: SqlitePool,
    topic_id: i64,
    provider: LLMProvider,    // §137.5: 新增
    model_name: &str,         // §137.5: 新增
) -> Result<(), String>
```

**`api_topic_extract_missing` + `api_topic_rebuild_dossier`** — Tauri command 接受 frontend 传的 provider + model_name:
```rust
pub async fn api_topic_extract_missing<R: Runtime>(
    app: tauri::AppHandle<R>,
    max_meetings: Option<i64>,
    provider: String,         // §137.5: 前端传 modelConfig.provider
    model_name: String,       // §137.5: 前端传 modelConfig.model
) -> Result<(usize, usize), String>
```

### 2. `frontend/src-tauri/src/summary/service.rs`

`process_transcript_background` 调 `trigger_after_summary` 处, 把 `provider: LLMProvider` + `model_name: String` 透传过去 (line 706-714):
```rust
let tg_provider = provider.clone();
let tg_model_name = model_name.to_string();
tokio::spawn(async move {
    crate::topic_graph::trigger_after_summary(
        tg_app, tg_pool, tg_meeting_id, tg_summary,
        tg_provider,           // §137.5: 用用户选的
        &tg_model_name,        // §137.5: 用用户选的
    ).await;
});
```

### 3. `frontend/src-tauri/src/topic_graph/scheduler.rs`

夜间重建 (P2-B) 也修了: 从 settings 表读用户当前 provider + model (不再硬编码):
```rust
let (provider, model_name) = match SettingsRepository::get_model_config(&pool).await {
    Ok(Some(setting)) => (setting.provider, setting.model),
    _ => ("ollama".to_string(), "llama3.2:latest".to_string()),  // 兜底
};
let llm_provider = LLMProvider::from_str(&provider).unwrap_or(LLMProvider::Ollama);
match super::rebuild_topic_dossier(app.clone(), pool.clone(), tid, llm_provider, &model_name).await { ... }
```

### 4. `frontend/src-tauri/src/live_qa/mod.rs`

`MODEL_NAME` 常量**删除**, `ask_live_qa` 接受 provider + model_name:
```rust
// pub const MODEL_NAME: &str = "qwen3.5:2b";  // §137.5: 删除 (硬编码)
pub async fn ask_live_qa(
    pool: SqlitePool,
    meeting_id: String,
    question: String,
    provider: LLMProvider,    // §137.5: 新增
    model_name: &str,         // §137.5: 新增
) -> Result<LiveQAResult, String>
```

`api_meeting_live_qa` Tauri command 同样接受 frontend 传的 provider + model_name:
```rust
pub async fn api_meeting_live_qa<R: Runtime>(
    app: AppHandle<R>,
    meeting_id: String,
    question: String,
    provider: String,         // §137.5: 前端传
    model_name: String,       // §137.5: 前端传
) -> Result<LiveQAResult, String>
```

加 helper:
```rust
fn app_data_dir_for_built_in_ai() -> Option<std::path::PathBuf> {
    if let Ok(d) = std::env::var("YANJINGAI_APP_DATA_DIR") {
        return Some(std::path::PathBuf::from(d));
    }
    if let Ok(home) = std::env::var("HOME") {
        return Some(std::path::PathBuf::from(home).join("Library/Application Support/tech.yanjingai.app"));
    }
    None
}
```

### 5. 前端 3 个调用方

| 文件 | 改动 |
|---|---|
| `frontend/src/app/knowledge/page.tsx` | 顶部加 `useConfig()`, `api_topic_extract_missing` invoke 加 `provider/modelName`, `api_topic_rebuild_dossier` invoke 也加 |
| `frontend/src/components/TopicSearch/TopicSearchModal.tsx` | 加 import `useConfig`, component 顶部调 `useConfig()`, `api_topic_rebuild_dossier` invoke 加 provider/modelName |
| `frontend/src/components/LiveQA/LiveQAOverlay.tsx` | 加 import `useConfig`, component 顶部调 `useConfig()`, `api_meeting_live_qa` invoke 加 provider/modelName |

3 处 invoke payload 都用 `provider: modelConfig.provider, modelName: modelConfig.model`。

---

## 验证

| 项 | 结果 |
|---|---|
| `cargo check --lib` | ✅ 0 errors (37 §18 warnings 不动) |
| `npx tsc --noEmit` | ✅ 1 个 §18 bun:test error (不动) |
| `cargo test --lib` | ✅ 364 passed / 0 failed / 3 ignored |
| `npx next build` | ✅ OK |
| `cargo build --release` | ✅ 3m08s, binary 73M, sha=6214c258832a |
| `bash scripts/sync_app_bundle.sh` | ✅ §93 + §98 + §108 + §99.6 全部 sync |
| `python3 scripts/check_historical_fixes.py` | ✅ **367/367 PASS** (新增 14 §137.5 anchor) |

guard 数量变化: 358 (handoff 上次) → 367 (本节): -5 (删除 3 §121 + 2 §137.3 硬编码 anchor) + 14 §137.5 新 anchor。

---

## 14 个新 guard anchor

| Anchor ID | 守卫目标 |
|---|---|
| `137_5_trigger_takes_provider_param` | topic_graph/mod.rs `trigger_after_summary` 签名含 `provider: LLMProvider` |
| `137_5_trigger_takes_model_param` | topic_graph/mod.rs `trigger_after_summary` 签名含 `model_name: &str` |
| `137_5_preflight_takes_provider_param` | topic_graph/mod.rs `preflight_llm_async` 签名含 `provider: &LLMProvider` |
| `137_5_extract_takes_provider_param` | topic_graph/mod.rs `extract_missing_topics` 接受 provider |
| `137_5_rebuild_takes_provider_param` | topic_graph/mod.rs `rebuild_topic_dossier` 接受 provider |
| `137_5_api_topic_extract_takes_provider` | topic_graph/mod.rs `api_topic_extract_missing` Tauri command 接受 provider String |
| `137_5_api_topic_rebuild_takes_provider` | topic_graph/mod.rs `api_topic_rebuild_dossier` Tauri command 接受 provider String |
| `137_5_live_qa_takes_provider` | live_qa/mod.rs `ask_live_qa` 接受 provider |
| `137_5_live_qa_api_takes_provider` | live_qa/mod.rs `api_meeting_live_qa` 接受 provider String |
| `137_5_knowledge_passes_modelconfig` | knowledge/page.tsx invoke 传 modelConfig.provider + modelConfig.model |
| `137_5_topicsmodal_passes_modelconfig` | TopicSearchModal.tsx invoke 传 modelConfig.provider + modelConfig.model |
| `137_5_liveqaoverlay_passes_modelconfig` | LiveQAOverlay.tsx invoke 传 modelConfig.provider + modelConfig.model |
| `137_5_summary_service_passes_provider` | summary/service.rs 调用 trigger_after_summary 传 tg_provider |
| `137_5_scheduler_uses_settings` | topic_graph/scheduler.rs 调 SettingsRepository::get_model_config (不依赖 frontend) |

rg 工具链改动: `scripts/check_historical_fixes.py::grep()` 加 `-U` 多行模式 flag, 让 `[\s\S]` 跨行匹配生效。

---

## §15 GUI 验收 (用户必做, 不能 CLI 测)

1. `killall meetily 2>/dev/null`
2. `open '/Users/wangwei/Documents/离线会记/target/release/言镜 AI.app'` (确认 mtime 13:26+)
3. 设置 → 模型 → 切换 provider / model (例如 Ollama + qwen3.5:4b 或 BuiltInAI + qwen2.5:3b)
4. 触发以下 3 个动作, 看后端日志都显示 `using {provider:?} model={model_name}` 而**不是固定** `qwen3.5:2b`:
   - 生成摘要 (trigger_after_summary)
   - 摘要完成后等几秒, 看 `[topic_graph]` 日志
   - Cmd+K 弹 Topic Search → 选某 topic → "重建档案" (rebuild_topic_dossier)
   - 会议详情页开会中 → ⌥+Space 弹 LiveQAOverlay → 提问 (live_qa)
5. 切换不同 model (例如 Ollama + llama3.2:latest) → 重做上述 3 步, 验证日志跟随 model

---

## §56 教训扩展

- **§137.3 (昨天) 我加了 BuiltInAI 兜底时硬编码了 `"qwen3.5:2b"`** — 用户当场指出
- 同样的反模式 `rebuild_topic_dossier` + `live_qa::ask_live_qa` 也有, 用户没报但**同 commit 必须修**, 否则 §137.5 留半截
- guard 13 个 §137.5 anchor 防回归, 任何未来 commit 把硬编码加回去都立刻暴露
- §56 教训: 写完一个 § 修复, **grep 全代码**确认同类反模式没漏

---

## commit

待 commit (5 个新文件改动 + 1 个 guard script + 1 个 outputs + 1 个 AGENTS.md):

```
git add \
  frontend/src-tauri/src/topic_graph/mod.rs \
  frontend/src-tauri/src/topic_graph/scheduler.rs \
  frontend/src-tauri/src/summary/service.rs \
  frontend/src-tauri/src/live_qa/mod.rs \
  frontend/src/app/knowledge/page.tsx \
  frontend/src/components/TopicSearch/TopicSearchModal.tsx \
  frontend/src/components/LiveQA/LiveQAOverlay.tsx \
  scripts/check_historical_fixes.py \
  AGENTS.md \
  outputs/§137.5-*.md
```

## 关联

- §137.3 (commit b961822, 8/19 早些) — BuiltInAI 兜底引入硬编码
- §137.4 (commit a667776, 8/19 早些) — thinking mode off
- §137.1 / §137.2 / §137.4 (前序 §137.x 修复)
- §18 (25+ cargo warnings + 1 bun:test 错误不动)
- §37 (硬闸门) / §56 (AGENTS.md 双校) / §92 (决策迁移铁律)
