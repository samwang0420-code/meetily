# C4 激活码 + C6 定价页 完整闭环 (2026-07-18)

## 核心数字
- selftest: **100/100 全过** (T1-T15, T12 = 激活码纯逻辑, T13 = 定价页, T14 = C4 完整链路, T15 = C6 三档)
- 端到端 selftest (4 套): **100 + 23 + 17 + 63 = 203 全过**
- Rust 测试: 9/9 (activation_code) + 28/28 (processor) 都过
- tsc: 0 新错误
- cargo check --lib: 0 errors (仅预存 15 个 dead-code warnings)

## C4 Pro 激活码链路

### 后端
- Migration `20260718000002_add_activation_codes.sql`:
  - 新表 activation_codes (code UNIQUE, tier, duration_days, expires_at, used_by_user_id, used_at, generated_by_operator, note, created_at)
  - 4 个索引 (code / used / expires / created_at)
  - users 加 activated_via_code 列 (audit trail)
- 仓储 `ActivationCodesRepository`:
  - insert / find_by_code / list_paginated / count / mark_used (并发安全) / revoke_unused
- 9 个 Rust 单测 (activation_code 纯逻辑): 生成 / 校验 / 大小写 / checksum bit-flip / 碰撞 / mask / Crockford 字符集安全

### Tauri IPC (4 个新命令, 全经 check_admin_token 鉴权)
- `admin_generate_activation_codes(operator_token, count, tier, duration_days, note) -> Vec<String>`: 批量生残 + 30 天 grace period
- `admin_list_activation_codes(operator_token, limit, offset) -> Vec<ActivationCodeRow>`: 分页查看
- `admin_revoke_activation_code(operator_token, code) -> u64`: 撤销未用的 (防泄漏)
- `user_redeem_activation_code(session, code) -> RedeemResult`: 5 类失败回退 (invalid_format / not_found / expired / already_used / db_error)

### 前端
- account page: 17 位激活码输入框 + 前置正则校验 + 调 `user_redeem_activation_code` + 失败 toast 友好化 (含 not_logged_in 引导登录)
- admin page: C4 管理面板 (生残表单 + 现有码列表含脱敏 + 撤销按钮)
- 集成到现有 refresh() 函数: 一次性拉 orders / leads / codes

## C6 独立定价页 + 引导弹窗

### /pricing/page.tsx
- **三档权限卡片**: 匿名 / 免费 / Pro (替代旧的二档对比)
- **匿名档说明**: 1 次 / 导出受水印 / 无长音频 / 无 Nano
- **免费档说明**: 每月 5 次会议 / 单次 ≤ 100 段 / 纪要带水印
- **Pro 完整权益**: 无限转录 + FunASR-Nano + cam++ 短时发言人 + 无水印 + 完整导出 + 实验性功能
- **内测期购买流程** 独立区块: 4 步流程 (邮件 → 24h 回复激活码 → app 内贴码 → v1.x 永久升级)
- FAQ 增至 8 个 (含内测期匿名规则 / 退款 7 天 / 多设备激活码改绑)
- 客服邮箱统一: lisangjie@icloudsend.com

### /app/_components/QuotaPaywallModal.tsx
- 已存在, 验证后无变更 (零死循环 / 含 0 骚扰 / 升级 CTA 含 / 三档对比含)
- 触发条件: anonymous_trial_exhausted / free_monthly_limit_reached

## 关键文件
- `meetily/frontend/src-tauri/migrations/20260718000002_add_activation_codes.sql` (新, 26 行)
- `meetily/frontend/src-tauri/src/user/activation_code.rs` (新, 220 行, 9 单测)
- `meetily/frontend/src-tauri/src/database/repositories/user.rs` (改, +80 行 ActivationCodesRepository)
- `meetily/frontend/src-tauri/src/user/commands.rs` (改, +180 行 4 IPC 命令)
- `meetily/frontend/src-tauri/src/lib.rs` (改, +4 行 IPC 注册)
- `meetily/frontend/src/app/pricing/page.tsx` (改, 105 → 261 行)
- `meetily/frontend/src/app/pricing/metadata.ts` (新)
- `meetily/frontend/src/app/account/page.tsx` (改, +65 行)
- `meetily/frontend/src/app/admin/page.tsx` (改, +95 行)
- `selftest/run.cjs` (重写, 100 条断言)

## 没做 (按 AGENTS.md §18 锁定)
- 法律/医疗垂直摘要模板
- LLM streaming / CoreML 路由
- cam++ 1h 实测
- Stripe / 微信/USDT 支付集成 (C8-C10)
- 实际 GUI session 重启验收 (用户)

## 风险 (silent abort 触发点)
- 改了 `commands.rs` IPC 命令表 (4 个新) → Tauri 命令解析器重编
- 改了 `lib.rs` 注册链
- 改了 `account/page.tsx` AuthContext 解构 (新加 `session`)
- 改了 `admin/page.tsx` state + 整段 UI

下次用户重启 app 后, 若遇到启动失败, 优先排查此 4 处回滚.
