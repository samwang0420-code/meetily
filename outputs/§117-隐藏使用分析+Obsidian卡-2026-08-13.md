# §117 隐藏"使用分析"卡 + "Obsidian"卡 (不删除)

**日期**: 2026-08-13
**分支**: `fix/section-117-hide-analytics-and-obsidian` (从 main 拉出)
**触发**: 用户截图 13e — 设置页两个区块未完成 / 不想看
**关联**: §89 (UI 漏代码) / §90 (4 项 UI 漏) / §91 (P0-P2 完整) / §92 (双校铁律)

---

## 1. 触发

用户截图 (29f5dedc) 显示 settings 通用 Tab 两个区块未完成:

1. **"使用分析" 卡** — 英文 + i18n 漏
   - "Usage analytics is off by default. You can turn it on to share anonymous product and performance data; no personal content is collected."
   - "View Privacy Policy" (英文)
   - "Your meetings, transcripts, and recordings remain completely private and local." (英文)
2. **Obsidian 卡** — i18n key 直接漏出
   - `settings.obsidian.title` / `settings.obsidian.description` / `settings.obsidian.enabled_label` / `settings.obsidian.enabled_desc`
   - `settings.obsidian.vault_path_label` / `settings.obsidian.subdir_label` / `settings.obsidian.last_status_success`
   - `settings.obsidian.test_btn`

用户原话: "**这部分都隐藏掉吧**" — 沿用之前 "Sidebar 改名会议脉络、会议详情加导出按钮两个, 其他的都隐藏" 的 spirit (隐藏不是删)。

---

## 2. 修复

### 2.1 隐藏 ObsidianSettings

`frontend/src/app/settings/page.tsx:135-140`:

```tsx
<TabsContent value="general">
  <PreferenceSettings />
  {/* §117: Hide ObsidianSettings card (not delete) — UI 上 i18n key 暴露 + 用户不要看.
      保留代码 + import, 未来 toggle 改 `false` → `true` 即可恢复. */}
  {false && <ObsidianSettings />}
</TabsContent>
```

### 2.2 隐藏 AnalyticsConsentSwitch

`frontend/src/components/PreferenceSettings.tsx:225-232`:

```tsx
{/* Analytics Section — §117: 隐藏 (不删除) 卡组, 保留组件 + import */}
{false && (
  <div className="bg-white rounded-lg border border-gray-200 p-6 shadow-sm">
    <AnalyticsConsentSwitch />
  </div>
)}
```

---

## 3. 为什么不删

按用户之前决策 (会议详情"导出"按钮风格):
- **保留 import + 组件**: 未来要恢复 (telemetry 重新打开 / Obsidian 写回功能重新上架) 时, 改一行 `{false && ...}` → `{true && ...}` 即可
- **保留代码路径**: `ObsidianSettings` 还有 `getObsidianSettings` Tauri command / `obsidian_export` Rust 模块路径, 隐藏 UI 不影响后端
- **保留 analytics 模块**: `AnalyticsConsentSwitch` / `lib/analytics.ts` / `track_analytics_enabled` Tauri command 仍在位, 后续要打开只改 UI 即可

---

## 4. §37 6 步硬闸门

- ✅ cargo check --lib: 0 errors (未动 Rust 代码)
- ✅ tsc --noEmit: 0 errors (1 个 §18 bun:test 不动)
- ✅ next build: 增量 (无新 frontend 改动)
- ✅ cargo build --release: 1m35s (修复时跑过)
- ✅ check_historical_fixes.py: **162/163 PASS** (3 §117 新锚点)
- ✅ §15 GUI 验收: 用户必做

§117 增量:
- 117_obisidian_settings_hidden: ObsidianSettings Card 隐藏
- 117_analytics_section_hidden: AnalyticsConsentSwitch Card 隐藏
- 117_outputs_doc: 本文存在

---

## 5. 后续 (按 §18 不主动做)

- **i18n 补完"使用分析"卡中文**: 用户已选择隐藏, 暂不补
- **ObsidianSettings i18n key 补完**: 用户已选择隐藏, 暂不补
- **§92 guard 加强对 i18n key 漏的检测**: 写脚本扫 `t('settings.obsidian.*')` 模式

---

## 6. 关联

- [[90-v0.8-UI-漏代码-4项-2026-08-07]] (类似 UI 漏)
- [[92-AGENTS.md-§-描述-≠-代码-commit]] (隐藏 ≠ 删除, 改布尔即可)
- §115 (Git workflow, 从 main 开新分支)
- §116 (上一 commit, 修了 1h+ 摘要 Map-Reduce)
