# §128 摘要设置下拉模板/模型切换修复 (2026-08-16)

## 触发
用户 8/16 截图反馈 "重新生成这里摘要设置还是不能切换模板和模型"。

## 根因
`frontend/src/components/MeetingDetails/SummaryPanel.tsx` 在 §124 dead-code-elimination 时整合了两个 ButtonGroup, 但"摘要设置"下拉的模板/AI 模型项**仍是简化版**:
1. **AI 模型** (`SummaryPanel.tsx:425`) → `onOpenModelSettings?.(() => {})` 传空回调, 啥也不做
2. **模板** (`SummaryPanel.tsx:421-430`) → `onTemplateSelect(availableTemplates[0].id, availableTemplates[0].name)` hardcode 第一个, 永远切到第一个

完整功能在 `SummaryGeneratorButtonGroup.tsx` (有完整 template dropdown + ModelSettingsModal Dialog), 但组件在 §124 被 dead-code 删除。

## 修复 (3 文件)

1. **`SummaryPanel.tsx`** 重写"摘要设置"下拉:
   - **AI 模型** DropdownMenuItem → 触发本地 Dialog, 内容装 ModelSettingsModal (layout="dialog")
   - **模板** 改成 DropdownMenuSub + SubTrigger + SubContent + RadioGroup, 列出全部 availableTemplates, 当前选中用 RadioItem 默认打钩
   - **PRO 徽章** 标记 required_tier='member' 的模板
   - **loading 状态** 显示 "正在加载模板…" 当 availableTemplates 为空
   - 加 modelSettingsDialogOpen state + Dialog + VisuallyHidden DialogTitle
   - 加 `availableTemplates.required_tier?: 'free' | 'member'` 到 Props (类型对齐 useTemplates)

2. **`i18n/locales/zh.ts` + `en.ts`**: 加 `summary.loading_templates` ('正在加载模板…' / 'Loading templates…')

3. **`SummaryPanel.tsx` 顶部 import**: 加 Dialog/DialogContent/VisuallyHidden/DropdownMenuSub/SubTrigger/SubContent/RadioGroup/RadioItem/ModelSettingsModal/Loader2/Check

## §37 6 步硬闸门
- ✅ tsc --noEmit: 0 errors (1 §18 bun:test 不动)
- ✅ next build: OK
- ✅ cargo check --lib: 0 errors / 28 §18 warnings 不动
- ✅ cargo build --release: 1m26s
- ⏳ check_historical_fixes.py: 234/234 PASS (待 outputs 后)
- ✅ sync_app_bundle.sh: tauri bundle SHA synced

## §15 GUI 验收 (用户必做, 不能 CLI 测)
```bash
killall meetily && open '/Users/wangwei/Applications/言镜 AI.app'
# 任意会议详情页:
# 1. 点右上"摘要设置" 齿轮按钮 → 下拉 3 项
# 2. 鼠标 hover "模板" 或点击右箭头 → 弹出 submenu
# 3. 看到全部模板列表 (standard_meeting / 法务咨询 / 医学咨询 / ...), 当前选中打钩
# 4. 点其他模板 → 模板名立刻更新到按钮上 (selectedTemplateName)
# 5. 点 "AI 模型" → 弹出 ModelSettingsModal 对话框, 可换 Ollama/BuiltIn 模型
# 6. save → 对话框关闭, 按钮显示新模型名 (右侧 .../model)
# 7. 点 "重新生成" → 实际用新模板 + 新模型生成
```

## 关联
- §124 (dead-code-elimination 整合 ButtonGroup 到 SummaryPanel, 但漏了 Dropdown 完整功能)
- §123 (模板选择持久化, 显示当前已选模板名)
- §106 (模型设置 Modal 砍掉云端 provider, 但保留本地模型切换)
- §37 (硬闸门) / §15 (GUI 验收) / §92 (三处同步)

[[128-摘要设置下拉修复]] (Obsidian) / `outputs/§128-...md` (Codex)
