# W1 P0 D3.1 — 行业词库精简 (法律 + 医疗) [2026-07-17]

## 用户决策
**砍掉**: tech / finance / education / cross_border / cross_border / 政务
**保留**: legal (法律诉讼) + medical (医疗会诊)
**额外保留**: 通用工程 / 跨行业 fallback 段 (前端/后端/数据库/接口/部署/架构 等) — 即便用户没选行业也能保底纠错

## 改动 4 个文件 + 1 个 L1 触发逻辑

### 1. `frontend/src-tauri/scripts/sherpa_hotwords.py`
- 删 `BUILTIN_HOTWORDS["tech"]` 整段 (~50 行, ~103 词)
- 删 `BUILTIN_HOTWORDS["finance"]` ~30 行
- 删 `BUILTIN_HOTWORDS["education"]` ~30 行
- 删 `BUILTIN_HOTWORDS["cross_border"]` ~45 行
- docstring `pack` enum 同步: `'none' | 'legal' | 'medical' | 'tech'` → `'none' | 'legal' | 'medical'`
- **净结果**: keys = ['legal', 'medical'], 96 + 88 词, 干净

### 2. `frontend/src-tauri/src/audio/industry_vocab.rs`
- `DOMAIN_TERMS` 删 `("tech", ...)` 条
- 新增 2 个单测:
  - `domain_lookup_is_exhaustive` 锁定 `[legal, medical]` (任何 PR 加行业必须改这里)
  - `domain_lookup_unknown_returns_empty` 旧 pack (`'tech'`/`'cross_border'`) 不 panic, 平滑降级

### 3. `frontend/src-tauri/scripts/sherpa_asr.py`
- `STATIC_HOMO` 字典里 `# ----- 研发 tech -----` 段重命名为 `# ----- 通用工程 / 跨行业通用 -----` (前端/后端/数据库/接口 等被错字"前段"/"数ju库" 等取代)
- **关键改动**: 删 `if words:` 门控 (line 408)
  - **旧**: 用户没选行业 (words=[]) → L1 整段被跳过 → "前段" 留在原文
  - **新**: L1 永远跑 → 即便 pack='none' 也能用通用工程段的同音表纠错
  - **误伤校验**: T6 验证 "我们今天讨论这个项目的技术架构和数据库设计" 没被改坏 (STATIC_HOMO 里 key 都是 ASR 误识别形式, 真实文本中很少出现)
  - 行号更新: 408 → 407 (去掉了一层缩进)

### 4. `frontend/src/app/settings/hotwords/page.tsx`
- `PACKS` 从 6 项改为 3 项
- 加注释说明法律 + 医疗是商业化对上线内容最敏感的行业

## 验证铁律 (按 AGENTS.md §15)
- ✅ Python `ast.parse` 4 个文件全过
- ✅ `cargo check`: 0 errors
- ✅ `tsc --noEmit`: 0 errors
- ✅ `cargo test --lib --`: **104 pass / 0 fail** (含新增 2 个 industry_vocab 单测)
- ✅ Python daemon 单元验证 6 用例:
  - T1: L0 永远触发 (BlockNote / 离线会记)
  - T2: L1 在 legal pack 触发 (管xiá权 → 管辖权)
  - T3: L1 在 medical pack 触发 (诊duàn → 诊断)
  - T4: **L1 通用工程 fallback 不依赖 pack** (前段→前端, 数ju库→数据库)
  - T5: 旧 pack=tech 降级平稳
  - T6: 误伤防护 — 普通文本 "技术架构和数据库设计" 不被改坏
- ✅ Tauri release binary 重新 build @ 21:57, 内嵌:
  - 3-PACKS hotwords HTML (10 kB)
  - 2-domain DOMAIN_TERMS Rust const
  - load_user_hotwords_into_globals 函数 (D3 hooks violation fix)
  - 通用工程 fallback 注释 / L1 触发逻辑 (运行时从 `sherpa_asr.py` 加载)

## 边界 / 已知

- **旧用户如果 DB 存了 pack='tech'**, 登录后 `get_hotwords('tech', '')` 返 `[]` → L0 + L1(通用) 仍生效, 没有任何 crash, 只是没了 tech 行业加成. 测试函数 `domain_lookup_unknown_returns_empty` 锁定降级路径.
- **旧录音 transcript 是修改前的 raw text**, DB 不会自动回溯. 重新转录会用新逻辑.
- **未来 D5 商业化 D5 全行业验收**: 只测法律 + 医疗两个真实场景, 不必测已删行业.

